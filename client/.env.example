# Frontend (client) env vars
# Base URL of your deployed backend API (no trailing slash, no /api)
# Important: N'ajoutez PAS /api à la fin - le code l'ajoute automatiquement
VITE_API_URL=https://your-backend.onrender.com