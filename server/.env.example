# Backend (server) env vars
NODE_ENV=production
PORT=5000
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME?schema=public
JWT_SECRET=replace-with-strong-secret
# Optional: CORS allowed origins (comma-separated)
CORS_ORIGINS=https://your-frontend-domain.vercel.app

# Maintenance (optionnel, à activer temporairement en prod si besoin de reset DB)
# Quand ALLOW_DB_RESET=true, l’endpoint POST /admin/reset-db est disponible et protégé par DB_RESET_TOKEN.
ALLOW_DB_RESET=false
DB_RESET_TOKEN=